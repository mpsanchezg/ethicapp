import React from 'react';
import PropTypes from 'prop-types';


export default WaitTask = ({ setTaskId, history }) => {
  // do polling to get task
  return (
    <></>
  );
};

WaitTask.propTypes = {
  setTaskId: PropTypes.func,
  history: PropTypes.any,
};

