import socket from 'socket.io';
import { Socket, Server } from 'socket.io';

// const createSocket = (server: Server) => {
//   return socket(server, {
//     path: '/websocket',
//   })
// };

// const addConductorEvent = (socket: Socket, eventName: string) => {
//   socket.on(eventName, () => {

//   })
// }
